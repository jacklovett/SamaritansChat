import { ChangePasswordDirective } from './password-match-validator';

describe('ChangePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
